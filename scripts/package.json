{
  "name": "vibes-scripts",
  "version": "1.0.0",
  "type": "module",
  "description": "Sync scripts for vibes-cli plugin",
  "scripts": {
    "sync": "node sync.js",
    "sync:force": "node sync.js --force"
  },
  "dependencies": {
    "esbuild": "^0.24.0"
  }
}
